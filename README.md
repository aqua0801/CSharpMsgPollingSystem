# CSharpMsgPollingSystem
MsgPollingSystem is a lightweight message polling library for .NET. It provides a simple request/response abstraction built on top of modern .NET concurrency primitives, with decent performance, correctness, and zero busy-looping.
